# Copy this file to .env and replace the placeholder before starting Compose.
# .env is ignored by Git.
POSTGRES_DB=zapasli
POSTGRES_USER=zapasli
POSTGRES_PASSWORD=REPLACE_WITH_A_RANDOM_64_CHARACTER_VALUE
APP_VERSION=0.1.0-dev
GIT_COMMIT=local
